export const environment = {
  production: true,
  apiBaseUrl: 'https://baobab-backend.fastapicloud.dev/api/v1',
  siteUrl: 'https://thebaobabgroup.org',
  /** Registry Console: sign out after this much inactivity (the server cookie TTL still applies). */
  consoleIdleTimeoutMinutes: 30,
};
